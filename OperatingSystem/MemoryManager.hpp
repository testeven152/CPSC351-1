

class MemoryManager {
    public:
        // MemoryManager& instance();
        // MemoryManager();
        // MemoryManager& operator=();
        void pageIn();
    private:
        // FreeFrameList freeFrames_;
        // MemoryManager();

};